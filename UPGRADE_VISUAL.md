# 合约升级可视化指南

## 🎯 核心问题：如何更新代理合约指向新地址？

**答案：调用 `proxy.updateLogicContract(newAddress)`**

---

## 📊 升级前后对比

### 升级前的状态

```
┌─────────────────────────────────────┐
│         用户/DApp                    │
│                                     │
│  使用固定地址: 0xe7f1...0512        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│      ProxyContract                  │
│      地址: 0xe7f1...0512            │
│  ┌───────────────────────────────┐  │
│  │ 状态变量 (存储)                │  │
│  │ ─────────────────────────────│  │
│  │ 槽 0: counter = 3            │  │
│  │ 槽 1: owner = 0xf39F...      │  │
│  │ 槽 2: logicContract =        │  │
│  │       0x5FbD...0aa3 ◄────────┼──┼─ 指向 V1
│  └───────────────────────────────┘  │
└──────────────┬──────────────────────┘
               │ delegatecall
               ▼
┌─────────────────────────────────────┐
│      LogicContract V1               │
│      地址: 0x5FbD...0aa3            │
│  ┌───────────────────────────────┐  │
│  │ 代码 (只读)                    │  │
│  │ ─────────────────────────────│  │
│  │ function increment() {       │  │
│  │     counter += 1;            │  │
│  │ }                            │  │
│  │                              │  │
│  │ function setCounter(uint) {  │  │
│  │     counter = value;         │  │
│  │ }                            │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

### 升级后的状态

```
┌─────────────────────────────────────┐
│         用户/DApp                    │
│                                     │
│  使用固定地址: 0xe7f1...0512        │
│  (地址没变！)                        │
└──────────────┬──────────────────────┘
               │
               ▼
┌─────────────────────────────────────┐
│      ProxyContract                  │
│      地址: 0xe7f1...0512 (不变)     │
│  ┌───────────────────────────────┐  │
│  │ 状态变量 (存储)                │  │
│  │ ─────────────────────────────│  │
│  │ 槽 0: counter = 3 (保留!)    │  │
│  │ 槽 1: owner = 0xf39F... (保留)│  │
│  │ 槽 2: logicContract =        │  │
│  │       0x5FC8...5707 ◄────────┼──┼─ 现在指向 V2
│  └───────────────────────────────┘  │
└──────────────┬──────────────────────┘
               │ delegatecall
               ▼
┌─────────────────────────────────────┐
│      LogicContract V2               │
│      地址: 0x5FC8...5707            │
│  ┌───────────────────────────────┐  │
│  │ 代码 (只读)                    │  │
│  │ ─────────────────────────────│  │
│  │ // V1 的功能                  │  │
│  │ function increment() {       │  │
│  │     counter += 1;            │  │
│  │ }                            │  │
│  │                              │  │
│  │ // 🆕 V2 新增功能             │  │
│  │ function incrementByMultiplier│  │
│  │ function decrement()         │  │
│  │ function reset()             │  │
│  │ function version()           │  │
│  └───────────────────────────────┘  │
└─────────────────────────────────────┘
```

---

## 🔄 升级过程详解

### 第 1 步：调用 updateLogicContract

```javascript
// JavaScript 代码
await proxy.updateLogicContract("0x5FC8...5707");
```

### 第 2 步：Solidity 执行

```solidity
// ProxyContract.sol
function updateLogicContract(address _newLogicContract) public {
    require(msg.sender == owner, "Only owner");
    
    // 这一行是关键！
    logicContract = _newLogicContract;
    //     ↓
    // 将存储槽 2 的值从 0x5FbD...0aa3 改为 0x5FC8...5707
}
```

### 第 3 步：存储变化

```
代理合约的存储：

升级前：
┌──────┬─────────────────┐
│ 槽 0 │ counter = 3     │
├──────┼─────────────────┤
│ 槽 1 │ owner = 0xf39F..│
├──────┼─────────────────┤
│ 槽 2 │ 0x5FbD...0aa3   │ ← V1 地址
└──────┴─────────────────┘

执行: logicContract = 0x5FC8...5707

升级后：
┌──────┬─────────────────┐
│ 槽 0 │ counter = 3     │ ← 不变
├──────┼─────────────────┤
│ 槽 1 │ owner = 0xf39F..│ ← 不变
├──────┼─────────────────┤
│ 槽 2 │ 0x5FC8...5707   │ ← 改变了！现在是 V2
└──────┴─────────────────┘
```

---

## 💡 关键理解

### 问：为什么状态会保留？

**答：因为状态存储在代理合约中，不在逻辑合约中！**

```
代理合约的存储：
┌──────────────────┐
│ counter = 3      │ ← 这个数据一直在代理合约里
│ owner = 0xf39F.. │
│ logicContract = ?│ ← 只是改变了这个指针
└──────────────────┘

逻辑合约 V1：
┌──────────────────┐
│ 只有代码         │ ← 没有实际数据
│ 没有状态数据     │
└──────────────────┘

逻辑合约 V2：
┌──────────────────┐
│ 只有代码         │ ← 没有实际数据
│ 没有状态数据     │
└──────────────────┘
```

### 问：升级后为什么新功能立即可用？

**答：因为 delegatecall 会从新地址读取代码！**

```
用户调用: proxy.incrementByMultiplier(5)
           ↓
代理合约: 我没有这个函数，触发 fallback
           ↓
fallback: 使用 delegatecall 调用 logicContract
           ↓
读取 logicContract 地址: 0x5FC8...5707 (V2)
           ↓
从 V2 地址加载 incrementByMultiplier 的代码
           ↓
在代理合约的上下文中执行这段代码
           ↓
修改代理合约的 counter: 3 + 5 = 8
```

---

## 🎬 完整升级时间线

```
时间 T0: 部署 V1
├─ LogicV1 地址: 0x5FbD...0aa3
└─ 代码: increment(), setCounter()

时间 T1: 部署代理
├─ Proxy 地址: 0xe7f1...0512
├─ logicContract = 0x5FbD...0aa3
└─ counter = 0

时间 T2-T5: 使用代理（V1 功能）
├─ proxy.increment() → counter = 1
├─ proxy.increment() → counter = 2
└─ proxy.increment() → counter = 3

时间 T6: 部署 V2
├─ LogicV2 地址: 0x5FC8...5707
└─ 代码: V1 功能 + 新功能

时间 T7: 执行升级 ⭐
├─ proxy.updateLogicContract(0x5FC8...5707)
├─ 交易确认
└─ 升级完成！

时间 T8: 验证
├─ proxy.logicContract() → 0x5FC8...5707 ✅
├─ proxy.counter() → 3 ✅ (状态保留)
└─ proxy.version() → "v2.0.0" ✅ (新功能可用)

时间 T9+: 使用新功能
├─ proxy.incrementByMultiplier(5) → counter = 8
├─ proxy.decrement() → counter = 7
└─ proxy.reset() → counter = 0
```

---

## 🔍 代码执行追踪

### 升级前调用 increment()

```
1. 用户: proxy.incrementViaDelegate()
   ↓
2. ProxyContract.incrementViaDelegate() {
       data = abi.encode("increment()")
       logicContract.delegatecall(data)  // logicContract = V1
   }
   ↓
3. 从 V1 地址 (0x5FbD...0aa3) 读取 increment() 代码
   ↓
4. 在 Proxy 上下文执行: counter += 1
   ↓
5. Proxy 的 counter: 2 → 3
```

### 升级后调用 incrementByMultiplier()

```
1. 用户: 发送交易到 proxy，调用 incrementByMultiplier(5)
   ↓
2. ProxyContract 没有这个函数，触发 fallback
   ↓
3. fallback() {
       delegatecall(logicContract, calldata)  // logicContract = V2
   }
   ↓
4. 从 V2 地址 (0x5FC8...5707) 读取 incrementByMultiplier() 代码
   ↓
5. 在 Proxy 上下文执行: counter += 5
   ↓
6. Proxy 的 counter: 3 → 8
```

---

## 📝 实际操作命令

### 方式 1: 使用 Hardhat 脚本

```bash
# 运行完整的升级演示
npx hardhat run scripts/upgrade-demo.js

# 输出会显示每一步的详细信息
```

### 方式 2: 使用 Hardhat Console

```bash
npx hardhat console
```

```javascript
// 1. 部署 V1
const LogicV1 = await ethers.getContractFactory("LogicContract");
const v1 = await LogicV1.deploy();
await v1.waitForDeployment();

// 2. 部署代理
const Proxy = await ethers.getContractFactory("ProxyContract");
const proxy = await Proxy.deploy(await v1.getAddress());
await proxy.waitForDeployment();

// 3. 使用 V1
await proxy.incrementViaDelegate();
await proxy.incrementViaDelegate();
console.log("Counter:", await proxy.counter()); // 2

// 4. 部署 V2
const LogicV2 = await ethers.getContractFactory("LogicContractV2");
const v2 = await LogicV2.deploy();
await v2.waitForDeployment();

// 5. 升级 ⭐ 关键步骤
await proxy.updateLogicContract(await v2.getAddress());

// 6. 验证
console.log("指向:", await proxy.logicContract());
console.log("Counter:", await proxy.counter()); // 2 (保留)

// 7. 使用 V2 新功能
const [owner] = await ethers.getSigners();
const data = v2.interface.encodeFunctionData("incrementByMultiplier", [10]);
await owner.sendTransaction({
  to: await proxy.getAddress(),
  data: data
});
console.log("Counter:", await proxy.counter()); // 12
```

### 方式 3: 运行测试

```bash
# 运行所有测试
npx hardhat test

# 只运行升级测试
npx hardhat test test/Upgrade.test.js

# 查看详细输出
npx hardhat test --verbose
```

---

## 🎓 总结

### 更新代理合约指向新地址的步骤：

1. **部署新的逻辑合约 V2**
   ```javascript
   const v2 = await LogicV2.deploy();
   ```

2. **调用代理合约的 updateLogicContract 函数** ⭐
   ```javascript
   await proxy.updateLogicContract(v2Address);
   ```

3. **完成！** 
   - 代理合约现在指向 V2
   - 所有状态保留
   - 新功能立即可用

### 这个过程改变了什么？

- ✅ 代理合约的 `logicContract` 变量（存储槽 2）
- ❌ 代理合约的其他状态（counter, owner 等）
- ❌ 代理合约的地址
- ❌ 用户的使用方式

### 为什么这样设计？

- 🔄 **可升级性**：可以修复 bug、添加功能
- 💾 **状态保留**：用户数据不会丢失
- 🎯 **地址不变**：用户无需更改配置
- ⚡ **立即生效**：升级后立即可用新功能
